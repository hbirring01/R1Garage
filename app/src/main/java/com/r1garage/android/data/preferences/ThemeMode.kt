package com.r1garage.android.data.preferences

/** App-wide theme preference. Mirrors the CreditCardApp/Stash setup so the
 *  ThemeMode-aware Theme.kt can stay unchanged. */
enum class ThemeMode { SYSTEM, LIGHT, DARK }
